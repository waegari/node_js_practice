let Article;_b46‍.x([["write",()=>write],["list",()=>list],["read",()=>read],["remove",()=>remove],["update",()=>update]]);_b46‍.w("../../models/article",[["default",["Article"],function(v){Article=v}]]);

       const write = async ctx => {
    const { title, subtitle, content, url } = ctx.request.body;
    const article = new Article({
        title: String,
        subtitle: String,
        content: String,
        url: String,
    });
    try {
        await article.save();
        ctx.body = article;
    } catch (e) {
        ctx.throw(500, e);
    }
};

       const list = ctx => {};

       const read = ctx => {};

       const remove = ctx => {};

       const update = ctx => {};